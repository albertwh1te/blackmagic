class Solution(object):
#      def firstUniqChar(self, s):
        #  for i in range(len(s)):
            #  w = s.count(s[i])
            #  if w == 1:
                #  return i
	#  return -1
    def firstUniqChar(self, s):
        for i in range(len(s)):
            
            
        

t = Solution()
print t.firstUniqChar("leetcode")
print t.firstUniqChar("aaaaaleetcode")
